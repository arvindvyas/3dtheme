<%= form_for(@mytheme) do |f| %>
  <% if @mytheme.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@mytheme.errors.count, "error") %> prohibited this mytheme from being saved:</h2>

      <ul>
      <% @mytheme.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
